# SQL New User Application

## 
Repo structure:

src/Provisioning/Provisioning.psm1 (all logic)

src/UI/App.xaml (layout)

src/UI/App.ps1 (loads XAML, wires buttons)

config/servers.json (server list + tags)

config/templates.json (role bundles)

logs/ (runtime output)
